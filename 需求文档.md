## 遥控器工具需求文档

### 1. 项目概述

#### 1.1 项目名称
**局域网智能遥控器（Web 版）**

#### 1.2 项目背景
用户在电脑上观看视频（如 B 站、YouTube 等）时，经常需要离开座位进行暂停、调整音量等操作，但现有解决方案（如蓝牙遥控器、手机 App）存在安装繁琐、平台兼容性差、功能单一等问题。本项目旨在通过手机浏览器访问电脑端 Web 服务，实现免安装、跨平台的遥控体验，并支持宏命令、AI 自然语言控制等高级功能。

#### 1.3 项目目标
- 提供基于局域网的手机端遥控界面，无需安装 App，打开浏览器即可使用。
- 支持对电脑的键盘、鼠标模拟输入，以及对浏览器播放器的精确控制。
- 提供可自定义的宏命令功能，支持录制与回放。
- 集成 AI 能力，允许用户通过自然语言控制电脑和浏览器。
- 保证低延迟、高稳定性和一定的安全性。

---

### 2. 目标用户与使用场景

#### 2.1 目标用户
- 经常在电脑上观看视频、进行演示或远程操作电脑的个人用户。
- 希望用手机代替传统遥控器或鼠标键盘的轻度用户。
- 对自动化操作（宏）和 AI 控制有需求的高级用户。

#### 2.2 核心使用场景
1. **视频播放控制**：用户坐在沙发上，用手机控制电脑浏览器的视频播放（播放/暂停、快进、音量、全屏等）。
2. **电脑远程鼠标键盘**：临时离开电脑，通过手机触摸板远程移动鼠标、输入文字。
3. **宏命令执行**：一键执行复杂操作序列，如“打开 B 站并自动播放追番列表”。
4. **AI 助手**：用自然语言下达指令，例如“播放下一集”、“把音量调到 50%”、“搜索周杰伦”。

---

### 3. 功能需求

#### 3.1 手机端（Web 页面）
| 功能模块 | 描述 |
|----------|------|
| 连接与配对 | 显示连接状态，首次访问输入配对码，自动建立 WebSocket 连接 |
| 播放控制 | 播放/暂停、上一集/下一集、快退/快进、音量加减、静音、全屏 |
| 触摸板 | 可切换的触摸板模式，支持单指移动鼠标、点击、双指滚动 |
| 键盘输入 | 提供简易键盘或文本输入框发送按键/文本 |
| 宏按钮 | 展示用户自定义的宏命令列表，点击一键执行 |
| AI 输入 | 底部输入框，支持文本或语音（可选）输入自然语言指令 |
| 状态显示 | 显示当前播放标题、平台、进度、音量等信息 |
| 设置 | 修改配对密码、触摸板灵敏度、宏管理（增删改） |

#### 3.2 电脑端（服务程序）
| 功能模块 | 描述 |
|----------|------|
| Web 服务器 | 托管手机端静态页面，提供 HTTP 访问 |
| WebSocket 服务 | 与手机端实时通信，接收命令、推送状态更新 |
| 输入模拟引擎 | 全局模拟键盘按键、鼠标移动/点击/滚动 |
| 宏引擎 | 宏的录制、存储、编辑、执行 |
| 浏览器扩展通信 | 与浏览器扩展建立本地 WebSocket 连接，转发控制指令 |
| AI 模块 | 调用本地/云端 LLM，解析自然语言为结构化命令并执行 |
| 配置管理 | 保存配对信息、宏配置、端口、AI 模型设置等 |

#### 3.3 浏览器扩展
| 功能模块 | 描述 |
|----------|------|
| 播放器控制 | 识别网页中的 `<video>`/`<audio>` 元素，执行播放/暂停、跳转、全屏等 |
| 平台适配 | 针对主流视频平台（B站、YouTube、腾讯视频等）进行特定元素操作 |
| 页面操作 | 支持打开网址、搜索、点击页面元素等 |
| 状态反馈 | 将当前播放状态回传给电脑端服务 |

#### 3.4 通信与安全
- 局域网内使用 WebSocket（`ws://`）进行实时双向通信。
- 首次连接需输入配对码，验证后返回 Token，后续请求携带 Token。
- 所有命令经过校验，防止非法操作。
- 可选：支持 HTTPS/WSS（自签名证书）以增强安全性。

---

### 4. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 指令从手机到电脑执行延迟 < 50ms（局域网） |
| 兼容性 | 电脑端支持 Windows 10/11；浏览器扩展支持 Chrome/Edge；手机浏览器支持主流移动端浏览器 |
| 可靠性 | 断线自动重连，指令丢失重试机制 |
| 易用性 | 界面简洁直观，重要功能一键可达 |
| 可扩展性 | 宏格式开放，AI 模块可替换不同模型 |
| 安全性 | 配对码 + Token 鉴权，命令白名单，限流防滥用 |

---

### 5. 技术选型

#### 5.1 电脑端
- **语言与框架**：C# / .NET 8（ASP.NET Core 自托管）
- **GUI**：WinForms + NotifyIcon（托盘图标，无主窗口）
- **Web 服务**：ASP.NET Core Kestrel，托管静态文件和 WebSocket
- **模拟输入**：InputSimulator 库（封装 Windows SendInput）
- **全局钩子**：globalmousekeyhook（用于宏录制）
- **浏览器扩展通信**：本地 WebSocket（扩展连接 `ws://127.0.0.1:8972`）
- **AI 集成**：OpenAI .NET SDK / HttpClient 调用 Ollama 本地模型
- **配置存储**：JSON 文件（简单）或 SQLite（如果需要复杂查询）

#### 5.2 手机端（Web 页面）
- **前端技术**：原生 HTML/CSS/JavaScript（或使用 Vue/React CDN 轻量引入）
- **通信**：WebSocket API
- **PWA**：可选支持添加到主屏幕，提供接近原生 App 的体验

#### 5.3 浏览器扩展
- **技术**：TypeScript + Chrome Extension Manifest V3
- **通信**：通过 `chrome.runtime.connect` 连接本地 WebSocket（或使用 Native Messaging，初期可用 WebSocket）

#### 5.4 开发工具
- IDE：Visual Studio 2022（电脑端）、VS Code（前端/扩展）
- 版本控制：Git
- 测试：Postman（WebSocket 调试）、浏览器 DevTools

---

### 6. 系统架构

```
┌─────────────┐          HTTP/WebSocket          ┌──────────────────────────────┐
│  手机浏览器   │ ◄──────────────────────────────► │      电脑端服务               │
│  (Web 页面)  │     JSON / MessagePack          │  - Kestrel Web 服务器         │
│  - 遥控面板  │                                  │  - WebSocket 服务             │
│  - 触摸板    │                                  │  - 输入模拟引擎                │
│  - 宏按钮    │                                  │  - 宏引擎                    │
│  - AI 输入   │                                  │  - 浏览器扩展通信              │
│  - 状态显示  │                                  │  - AI 模块                   │
└─────────────┘                                  └──────────┬───────────────────┘
                                                             │ 本地 WebSocket
                                                             ▼
                                            ┌──────────────────────────────┐
                                            │  浏览器扩展 (Chrome/Edge)     │
                                            │  - 控制网页播放器             │
                                            │  - 页面元素操作               │
                                            └──────────────────────────────┘
```

**核心设计原则**：
- 手机端只与电脑端服务通信，不直接与浏览器扩展通信。
- 电脑端服务作为中枢，负责路由指令、执行本地模拟输入、调用扩展或 AI。
- 局域网内无中心服务器，电脑端即服务端。

---

### 7. 详细设计

#### 7.1 电脑端服务模块

##### 7.1.1 Web 服务器
- 使用 `WebApplication.CreateBuilder()` 配置 Kestrel 监听 `0.0.0.0:5000`（可配置端口）。
- 托管静态文件：手机页面文件放在 `wwwroot` 目录。
- 添加 WebSocket 中间件，路径 `/ws`。

##### 7.1.2 WebSocket 消息处理
- 消息格式采用 JSON：
  ```json
  {
    "id": "uuid",
    "type": "command | event | response",
    "action": "play | pause | mouse_move | macro_execute | browser_action | ai_query",
    "payload": { ... },
    "timestamp": 1694070000000
  }
  ```
- 手机端发送 `command`，电脑端执行后返回 `response`；电脑端主动推送 `event`（如播放状态更新）。
- 心跳机制：每 30 秒发送 ping/pong 保持连接。

##### 7.1.3 输入模拟引擎
- 封装接口：
  ```csharp
  public interface IInputSimulator
  {
      void SendKey(VirtualKeyCode key, ModifierKeys modifiers = ModifierKeys.None);
      void SendText(string text);
      void MouseMove(int dx, int dy);
      void MouseClick(MouseButton button);
      void Scroll(int amount);
  }
  ```
- 实现基于 `InputSimulator` 库，注意权限（需以管理员身份运行才能模拟全局按键）。

##### 7.1.4 宏引擎
- 宏存储为 JSON 文件，格式示例：
  ```json
  {
    "id": "macro_1",
    "name": "打开B站并播放",
    "steps": [
      { "type": "key_combo", "keys": ["win", "r"], "delay_after": 200 },
      { "type": "text_type", "text": "https://www.bilibili.com", "delay_after": 300 },
      { "type": "key_press", "key": "enter", "delay_after": 5000 },
      { "type": "browser_action", "action": "play", "delay_after": 1000 }
    ],
    "loop": false
  }
  ```
- 支持步骤类型：`key_press`、`key_combo`、`text_type`、`mouse_move`、`mouse_click`、`scroll`、`delay`、`browser_action`、`wait_for_condition`、`execute_macro`、`repeat`。
- 录制功能：使用 `globalmousekeyhook` 监听全局键盘鼠标事件，记录时间戳并生成步骤。
- 提供编辑接口（通过 Web 页面或配置文件手动修改）。

##### 7.1.5 浏览器扩展通信
- 电脑端启动第二个 WebSocket 服务，监听 `127.0.0.1:8972`。
- 扩展的 background script 连接该地址。
- 电脑端将手机端发来的 `browser_action` 命令转发给扩展，扩展执行后返回结果。
- 如果扩展未连接，则回退为模拟按键（如空格键播放/暂停）。

##### 7.1.6 AI 模块
- 接收手机端 `ai_query` 命令。
- 使用预设的 system prompt 调用 LLM（OpenAI 或 Ollama）。
- LLM 返回结构化 JSON 命令，经白名单校验后执行。
- 支持多轮对话上下文（可选，初期可单轮）。
- 执行结果回传手机端。

##### 7.1.7 配置管理
- 配置文件 `config.json` 存储：
  - 服务端口
  - 配对密码（或 Token）
  - 宏定义
  - AI 模型配置（API Key、本地模型地址等）
  - 触摸板灵敏度等
- 托盘图标提供菜单：显示 IP 和端口、修改配对密码、打开配置文件夹、退出。

#### 7.2 手机端页面
- **目录结构**（`wwwroot`）：
  ```
  wwwroot/
  ├── index.html
  ├── css/
  │   └── style.css
  ├── js/
  │   ├── main.js       // 主逻辑
  │   ├── ws.js         // WebSocket 封装
  │   └── ui.js         // UI 渲染与交互
  └── manifest.json     // PWA 可选
  ```
- **页面布局**（自上而下）：
  1. 状态栏（连接状态、设备名、音量）
  2. 播放信息卡（封面、标题、进度条）
  3. 控制按钮网格（播放控制）
  4. 触摸板切换 + 触摸板区域（默认隐藏）
  5. 宏按钮区域
  6. AI 输入栏
- **交互逻辑**：
  - 所有按钮点击通过 WebSocket 发送对应 `command`。
  - 触摸板触摸事件转换为鼠标移动指令，按灵敏度缩放。
  - 收到 `event` 更新 UI（如播放状态、音量）。
  - 断线时显示重连提示，自动重试。

#### 7.3 浏览器扩展
- **Manifest V3** 配置，权限：`tabs`、`activeTab`、`scripting`、`storage`。
- **background.js**：
  - 连接电脑端本地 WebSocket。
  - 接收指令，根据 `action` 类型通过 `chrome.tabs.sendMessage` 或 `chrome.scripting.executeScript` 操作当前标签页。
  - 定期查询当前播放状态，发送给电脑端。
- **content script**：
  - 注入到所有页面，识别 `<video>`/`<audio>` 元素。
  - 暴露 `window.__remoteControl` 对象，提供播放控制函数。
  - 平台特定逻辑（可选，通过判断域名加载不同脚本）。

---

### 8. 接口与通信协议

#### 8.1 手机端 ↔ 电脑端
- **连接建立**：手机访问 `http://<电脑IP>:5000`，页面加载后自动连接 `ws://<电脑IP>:5000/ws`。
- **认证**：首次连接需发送 `auth` 命令，包含配对码；电脑验证后返回 `token`，后续消息在 `Authorization` 头或消息字段中携带。
- **命令示例**：
  ```json
  // 播放/暂停
  { "id": "1", "type": "command", "action": "play_pause", "payload": {} }
  // 鼠标移动（相对位移）
  { "id": "2", "type": "command", "action": "mouse_move", "payload": { "dx": 10, "dy": -5 } }
  // 执行宏
  { "id": "3", "type": "command", "action": "macro_execute", "payload": { "macro_id": "macro_1" } }
  // AI 指令
  { "id": "4", "type": "command", "action": "ai_query", "payload": { "text": "播放下一集" } }
  ```
- **事件推送**：
  ```json
  // 播放状态更新
  { "id": "5", "type": "event", "action": "playback_state", "payload": { "title": "...", "platform": "bilibili", "is_playing": true, "volume": 80, "current_time": 123.4, "duration": 600 } }
  ```

#### 8.2 电脑端 ↔ 浏览器扩展
- 使用本地 WebSocket（`ws://127.0.0.1:8972`），消息格式同上，但去掉认证（仅限本机）。
- 电脑端发送 `command`，扩展返回 `response`。

#### 8.3 AI 模块
- 电脑端收到 `ai_query` 后调用 LLM API。
- 系统提示词定义命令空间，要求输出 JSON。
- 返回的 JSON 经安全校验后执行。

---

### 9. 安全设计

- **配对码与 Token**：电脑端启动时生成随机 6 位配对码，显示在托盘提示或日志中。手机首次连接输入配对码，验证通过后签发 Token（可设置有效期），后续请求携带 Token。
- **命令白名单**：定义允许执行的命令列表，AI 生成的命令也须匹配白名单。
- **敏感操作确认**：执行宏、打开外部程序等可要求手机端二次确认。
- **限流**：对触摸板移动等高频事件限流（如每秒最多 30 条）。
- **网络隔离**：默认只监听局域网，不暴露公网。
- **HTTPS/WSS**：可选，通过自签名证书 + 指纹校验提升安全性。

---

### 10. 开发计划与里程碑

#### 阶段一：MVP（1-2 周）
- 目标：实现手机浏览器控制电脑模拟按键。
- 任务：
  - 搭建电脑端 ASP.NET Core 项目，托管静态页面。
  - 实现 WebSocket 基础通信。
  - 集成 InputSimulator，支持空格（播放/暂停）、方向键等。
  - 手机页面提供基础按钮，发送对应命令。
- 交付：可用的遥控器原型。

#### 阶段二：浏览器扩展（1 周）
- 目标：实现浏览器播放器精确控制。
- 任务：
  - 开发 Chrome 扩展，连接电脑端本地 WebSocket。
  - 实现 `<video>` 元素控制。
  - 电脑端检测扩展连接，优先使用扩展控制。
- 交付：扩展 + 电脑端支持。

#### 阶段三：宏系统（3-5 天）
- 目标：支持宏录制与执行。
- 任务：
  - 集成 globalmousekeyhook 实现录制。
  - 设计宏存储格式。
  - 手机端展示宏按钮并支持执行。
- 交付：宏功能可用。

#### 阶段四：AI 集成（1 周）
- 目标：支持自然语言控制。
- 任务：
  - 接入 OpenAI / Ollama。
  - 定义系统提示词。
  - 手机端增加 AI 输入框。
  - 命令解析与执行。
- 交付：AI 控制功能。

#### 阶段五：优化与发布（1 周）
- 任务：
  - UI 美化与响应式优化。
  - 错误处理、断线重连。
  - 打包电脑端为单文件 exe。
  - 编写使用文档。
- 交付：可分发版本。

---

### 11. 测试计划

- **单元测试**：
  - 宏引擎解析与执行。
  - WebSocket 消息解析。
  - 输入模拟函数（Mock）。
- **集成测试**：
  - 手机端页面与电脑端服务联调。
  - 电脑端与浏览器扩展通信。
  - AI 命令解析与执行链路。
- **性能测试**：
  - 鼠标移动延迟（局域网）。
  - 大量消息下的稳定性。
- **兼容性测试**：
  - 不同手机浏览器（Chrome、Safari、Edge）。
  - 不同 Windows 版本。
  - 不同视频平台（B站、YouTube、腾讯视频等）。
- **安全测试**：
  - 未授权访问尝试。
  - 命令注入测试。

---

### 12. 部署与发布

- **电脑端**：
  - 发布为单文件可执行程序（.NET 8 的 `PublishSingleFile`）。
  - 首次运行自动生成配置文件，并在托盘显示。
  - 提供简单的安装包（可选，使用 Inno Setup 或 NSIS）。
- **手机端**：
  - 静态文件随电脑端程序打包，无需单独部署。
  - 可选：部署到局域网 Web 服务器或内网穿透服务以支持广域网。
- **浏览器扩展**：
  - 开发版通过 Chrome 开发者模式加载。
  - 正式版发布到 Chrome Web Store（可选）。

---

### 13. 前期准备工作

#### 13.1 开发环境搭建
- **电脑端**：
  - 安装 .NET 8 SDK。
  - 安装 Visual Studio 2022（或使用 VS Code + C# 扩展）。
  - 创建 ASP.NET Core Web 空项目。
- **手机端**：
  - 任意文本编辑器或 VS Code。
  - 现代浏览器（Chrome/Edge）用于调试。
- **浏览器扩展**：
  - VS Code + TypeScript。
  - Chrome 浏览器（开发者模式）。

#### 13.2 依赖库安装
- 电脑端 NuGet 包：
  - `InputSimulator`（或 `InputSimulatorCore`）
  - `globalmousekeyhook`（可能需要从 GitHub 添加）
  - `Newtonsoft.Json`（JSON 处理）
  - `Microsoft.AspNetCore.WebSockets`（内置）
  - OpenAI SDK（可选）或 `HttpClient`（调用 Ollama）
- 前端：无额外依赖（原生 JS）。

#### 13.3 项目结构建议
```
remote-control/
├── server/                     # 电脑端 .NET 项目
│   ├── RemoteControl.Server.csproj
│   ├── Program.cs
│   ├── appsettings.json
│   ├── wwwroot/                # 手机页面
│   ├── Services/
│   │   ├── WebSocketHandler.cs
│   │   ├── InputSimulatorService.cs
│   │   ├── MacroService.cs
│   │   ├── BrowserBridge.cs
│   │   └── AIService.cs
│   └── Models/
│       ├── CommandMessage.cs
│       └── MacroDefinition.cs
├── extension/                  # 浏览器扩展
│   ├── manifest.json
│   ├── background.js
│   └── content.js
└── docs/                       # 文档
    └── requirements.md
```

#### 13.4 关键技术验证
- 验证 InputSimulator 在目标系统上的权限和效果。
- 验证浏览器扩展与本地 WebSocket 通信可行性。
- 验证 Ollama 或 OpenAI API 的可用性。

---

### 14. 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 模拟输入权限问题 | 无法控制部分应用 | 运行时请求管理员权限，提示用户 |
| 浏览器平台差异 | 扩展无法覆盖所有网站 | 优先支持主流平台，提供回退模拟按键 |
| AI 命令解析错误 | 执行错误操作 | 命令白名单 + 用户确认 |
| 局域网 IP 变动 | 手机无法连接 | 支持 mDNS 广播或显示当前 IP |
| 性能瓶颈 | 触摸板延迟 | 优化消息频率，使用二进制协议（如 MessagePack） |

---

**文档版本**：v1.0  
**编写日期**：2026-09-06  
**适用范围**：开发团队、项目规划